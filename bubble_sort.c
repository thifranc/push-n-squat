
//BUBBLE SORT :

/*
t_list		*next_a_elem(t_list **list, t_data data)
{
	t_list	*tmp;
	
	tmp = *list;
	while (belongs_to(tmp, data.goal, data.pivot) < 0)
		tmp = tmp->next;
	return (tmp);
}
*/
				/*if (*a == list_max(&(*a)))
					rotate(&(*a));
				else
				{
					if ((*a)->nbr > next_a_elem(&((*a)->next), data)->nbr)
						swapp(&(*a));
					else
					{
						rotate(&(*a));
					}
				}

				
*/
